# Models

In this directory (or its subfolders), `SCADarina` will put your uploaded, and altered files (when saved from the UI).

> [!IMPORTANT]  
> Files you entered through `open file` won't be saved there automatically; save them from the UI, or upload directly in the filebrowser to have them available later.